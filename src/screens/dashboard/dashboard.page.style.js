import { StyleSheet } from 'react-native';

const pageStyle = StyleSheet.create({
  signUpViewStyles: {
    position: 'absolute',
    bottom: 60,
    width: '100%',
    paddingHorizontal: 32,
    paddingVertical: 16,
  },
});

export { pageStyle };
